# Frontend datasource mode
# local: LocalOutfitRepository
# api: ApiOutfitRepository (requires backend endpoints implementation)
NEXT_PUBLIC_DATA_SOURCE=local

# Frontend -> Backend API base URL (used when NEXT_PUBLIC_DATA_SOURCE=api)
NEXT_PUBLIC_API_URL=https://outfit-archive-backend.onrender.com
